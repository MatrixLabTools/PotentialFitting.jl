language: julia

os:
  - linux
dist: xenial

julia:
  - 1.0
  - 1.1
  - nightly

notifications:
  email: false

codecov: true

matrix:
    allow_failures:
        - julia: nightly

before_install:
  - sudo apt-get install python3-sklearn
